import { Input } from '@chakra-ui/react'
import { todoListState } from '../components/atom'

const InputTodo = (title: string, handleChange: any) => {
  return (
    <Input />
  )
}

export default InputTodo
